feat: Exclude quiz page from ad activation

Updates the AdZepActivator component to prevent ad units from loading on the quiz page (`/quiz`). This ensures a cleaner user experience for users who have clicked on an advertisement to take the quiz.
