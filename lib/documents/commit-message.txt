chore(package): sync package metadata and lockfile

- Update package.json (scripts/metadata housekeeping)
- Regenerate pnpm-lock.yaml to reflect current dependency graph

No app logic changes. Part of routine maintenance to keep project scripts and lockfile in sync.
