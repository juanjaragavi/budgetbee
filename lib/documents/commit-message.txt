fix: Resolve Tailwind CSS syntax errors and improve styling

- Fixed incorrect !important modifier syntax in components.css
- Corrected rgba color values by removing spaces (rgba(0,0,0,0.125))
- Fixed responsive modifier order (md:!-mx-8 instead of !md:-mx-8)
- Resolved all console errors preventing page from loading
- Homepage now displays correctly with all blog sections

This fixes critical CSS parsing errors that were preventing the site from rendering properly.
