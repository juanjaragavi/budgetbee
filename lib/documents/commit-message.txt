fix(ads): remove non-existent min-h-auto/min-size-auto classes; keep neutral wrappers

- Ad wrapper cleanup across pages:
	- src/pages/credit-card-recommender-p1.astro: drop `min-h-auto` from us_budgetbeepro_1 and _2 containers
	- src/pages/credit-card-recommender-p2.astro: drop `min-h-auto` from us_budgetbeepro_1 and _2 containers
	- src/pages/credit-card-recommender-p3.astro: drop `min-h-auto` from us_budgetbeepro_1 and _2 containers
- Shortcode fix:
	- src/layouts/shortcodes/AdZone.tsx: correct typo `min-size-auto` -> `min-w-auto`

Reasoning:
- Tailwind doesn't have `min-h-auto` or `min-size-auto`; they caused ineffective styles.
- Keeping `min-w-auto flex justify-center items-center` preserves neutral ad placement without forcing height.

Verification:
- Visual check in dev: ad containers render without layout shifts; no Tailwind warnings for unknown classes.
